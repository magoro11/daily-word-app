import { Pressable, ScrollView, StyleSheet, Switch, Text, View } from 'react-native'
import { BIBLE_INTERVALS, QUOTE_INTERVALS } from '@/core/types'
import { useAppStore } from '@/store/useAppStore'
import { requestNotificationPermission } from '@/utils/notifications'

export function SettingsPanel() { const settings = useAppStore(s => s.settings); const update = useAppStore(s => s.updateSettings); return <ScrollView contentContainerStyle={styles.page}><Text style={styles.title}>Settings</Text><Section title="Scripture"><Toggle label="Show Bible verses" value={settings.bibleEnabled} onChange={(v: boolean) => update({ bibleEnabled: v })} /><Choices value={settings.bibleIntervalMs} items={BIBLE_INTERVALS} onChange={(v: number) => update({ bibleIntervalMs: v })} /></Section><Section title="Motivation"><Toggle label="Show motivational quotes" value={settings.motivationEnabled} onChange={(v: boolean) => update({ motivationEnabled: v })} /><Choices value={settings.motivationIntervalMs} items={QUOTE_INTERVALS} onChange={(v: number) => update({ motivationIntervalMs: v })} /></Section><Section title="Appearance"><Toggle label="Dark mode" value={settings.theme === 'dark'} onChange={(v: boolean) => update({ theme: v ? 'dark' : 'light' })} /><Toggle label="Daily themed content" value={settings.dailyThemeEnabled} onChange={(v: boolean) => update({ dailyThemeEnabled: v })} /></Section><Section title="Notifications"><Toggle label="Notify on new Bible verse" value={settings.notificationsEnabled} onChange={async (v: boolean) => { if (v) await requestNotificationPermission(); update({ notificationsEnabled: v }) }} /><Toggle label="Notify on new motivation" value={settings.notifyOnQuoteChange} onChange={(v: boolean) => update({ notifyOnQuoteChange: v })} /></Section><Section title="Schedule"><Toggle label="Use active hours" value={settings.scheduleEnabled} onChange={(v: boolean) => update({ scheduleEnabled: v })} /><Text style={styles.hint}>The default weekday schedule is 7:00 AM to 10:00 PM. Customize individual days in a desktop build.</Text></Section></ScrollView> }
function Section({ title, children }: { title: string; children: any }) { return <View style={styles.section}><Text style={styles.sectionTitle}>{title}</Text>{children}</View> }; function Toggle({ label, value, onChange }: { label: string; value: boolean; onChange: (value: boolean) => void }) { return <View style={styles.row}><Text style={styles.label}>{label}</Text><Switch value={value} onValueChange={onChange} trackColor={{ false: '#CBD4CD', true: '#6A907B' }} /></View> }; function Choices({ value, items, onChange }: any) { return <View style={styles.choices}>{items.map((item: any) => <Pressable key={item.value} onPress={() => onChange(item.value)} style={[styles.choice, value === item.value && styles.selected]}><Text style={[styles.choiceText, value === item.value && styles.selectedText]}>{item.label}</Text></Pressable>)}</View> }
const styles = StyleSheet.create({ page: { padding: 26, gap: 16, maxWidth: 900, width: '100%', alignSelf: 'center', paddingBottom: 40 }, title: { color: '#25352D', fontSize: 26, fontWeight: '700', marginBottom: 3 }, section: { backgroundColor: '#FFF', borderRadius: 8, borderWidth: 1, borderColor: '#E0E4DC', paddingHorizontal: 17, paddingTop: 15, paddingBottom: 10 }, sectionTitle: { color: '#315644', fontSize: 12, fontWeight: '800', letterSpacing: 1, marginBottom: 3 }, row: { minHeight: 50, flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center', borderBottomWidth: 1, borderColor: '#EFF1EE' }, label: { color: '#39483F', fontSize: 15 }, choices: { flexDirection: 'row', flexWrap: 'wrap', gap: 7, paddingTop: 12, paddingBottom: 7 }, choice: { paddingVertical: 8, paddingHorizontal: 10, borderWidth: 1, borderColor: '#D7DED8', borderRadius: 5 }, selected: { backgroundColor: '#355A49', borderColor: '#355A49' }, choiceText: { color: '#617168', fontSize: 12, fontWeight: '600' }, selectedText: { color: '#FFF' }, hint: { color: '#758178', lineHeight: 19, fontSize: 13, paddingVertical: 11 } })
